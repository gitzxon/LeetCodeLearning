package array.ShortestSubarrayWithSumAtLeastK;


// todo : 忘了写了。抽空补上。
class Solution {
    public int shortestSubarray(int[] nums, int k) {

        int len = nums.length;
        int[] dp = new int[len];

        for (int subarrayLen = 1; subarrayLen <= len; subarrayLen++) {
            
        }
        return -1;
    }
}