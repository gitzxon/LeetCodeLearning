package study.stock.BestTimeToBuyAndSellStock.IV

import org.junit.Test

class SolutionNewTest {
    @Test
    fun maxProfit() {
        SolutionNew().maxProfit(
                2, intArrayOf(2, 4, 1)
        )
    }
}